import React from 'react';

const Header = () => {

    return (
        <div style={{display: 'flex', width: '100%', justifyContent: 'center', alignItems: 'center', backgroundColor: 'black', color: 'white', height: '50px'}}>
            Это мой первый React. проект!
        </div>
    );
};

export default Header;